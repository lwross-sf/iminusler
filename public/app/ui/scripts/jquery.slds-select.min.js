function selectInput(data, value, label){
  var htm = '<option value="0" disabled="">Select One...</option>';
  for (i = 0; i < data.length; i++) {
    htm += '<option value="' + data[i][value] + '">' + data[i][label] + '</option>'
  }
  return htm
}